# SDD ledger — plan: docs/superpowers/plans/2026-09-16-tenancy-workflows.md

Prepared during F4. W1 is currently in implementation recovery on reviewed75c8eff with /root/tenancy_w1_recovery; all later workflows wait for their stated prerequisites. All briefs now include shared context, and each still needs its current reviewed head/API handoff before dispatch.

## Preflight task consistency
| Task | Consistency / decision |
|---|---|
| W1 | Activity root captures ambient tenant at recording; historical subject optional, operational Platform rows separate from catalog. Own automatic Spatie capture as well as explicit recorder. |
| W2 | Canonical Form owns all children; public admission precedes form/locale/token resolution. Version 2 tokens and scalar callback capture apply together. |
| W3 | Template copy owns orchestrated graph; Content owns its added copy seam, Media owns stages/root rollback cleanup. Full dependencies required before dispatch. |
| W4 | Target ownership and global-principal projection are distinct; allowed global mentions need host/Auth adapter without Comments-to-Auth dependency. |
| W5 | Real factory test explicitly consumes tenant Settings repository, so U3 must complete before W5. Platform credentials remain deployment-managed. |
| W6 | Graph retention/adoption follows all workflow adapters, actual engines/storage/workers; cannot mark ready from W1 alone. |

## Shared-file/interface pairs
| Pair | Shared boundary | Check |
|---|---|---|
| W1/W6 | Activity registrar/adapter/retention | W1 provides root ownership and empty adoption; W6 adds full existing-data/lifecycle proof without new marker system. |
| W2/W6 | Forms registrar/adapter/callbacks | Scalar captured callbacks are package boundary; lifecycle must consume real Action behavior. |
| W3/C1-C6/R4 | Content copy seam and MediaCatalogImport | Copy access lives in Content with Templates class-name implementation; no reverse dependency. R4 stages/persists under shared root transaction. |
| W3/W6 | Template grant/render/adoption | Completed copies independent of sources; retention must preserve exact output-path ownership. |
| W4/F4 | Polymorphic target inheritance | Use reviewed internal allowlisted parent resolver seam; globally registered model alone is insufficient. |
| W4/A3-A10 | Global-principal mention projection | Host/Auth supplies membership-safe projection; Comments never enumerates arbitrary global principals. |
| W5/U3 | Factory reads tenant Settings | Reorder the independent late task group so U3 precedes W5; no mock replaces exact SMTP content proof. |
| W5/F7 | Call/failed before deserialization | Preserve native framework boundary and scoped factory resolution; Platform envelopes forbidden. |
| W6/F2 | Cleanup and suspension | Maintenance runner synchronous exact-tenant lease; ordinary jobs cannot inherit recovery privilege. |
| W1-W6/F5 | Adoption fixture/protocol | Real coordinator, no RefreshDatabase outer transaction or manual active markers; local test fixture only. |

Ruling: Execute U3 before W5 within the program's independent late W/U group — W5's required real message-content fixture explicitly reads tenant Settings — if wrong, later packages wait for a safe prerequisite rather than shipping a partial global fallback.

Ruling: The planning-only sentences describe the former review turn and do not block this execution — the user explicitly said do it after approving this program — if wrong, all changes remain local/reversible and no merge/push/publish occurs.

Pre-dispatch checks still required: package fixture/provider defaults against actual reviewed F6 compatibility enforcement; exact installed method defaults (including Comments audience argument) must be used rather than copying illustrative snippets blindly. Host-side bounded tenant enumeration for scheduler/retention commands is not a capability of the frozen find-only TenantDirectory contract; implement these commands with explicit reviewed scalar tenant worklists or an independently authorized host enumerator, never by guessing tenant IDs or querying domain payloads across tenants. Default package-directory listing, if needed, must be a narrow privileged read only.

During F6 implementation, W1 brief now includes exact global/order/concrete fixture setup plus reviewed mixed discriminator and real adoption requirements. Current F6/F7/F8/U1/U2 handoff still required before dispatch. W2–W6 extracts remain raw; no workflow implementation started.

During F6 implementation, controller appended exact global/shared interface/test setup context to remaining briefs 2,3,4,5,6. These are prepared requirements only, not dispatched or completed. Before EACH dispatch add current reviewed dependency/API handoff and task-specific preflight rulings; read ledger. Full source plan need not be reread by worker.

Ruling: W1 proves Activity recording and deferred capture for the existing value-free Settings subject protocol (`nvl_setting` plus scalar id) using package-local model-free fixtures; U3 owns the real tenant SettingRepository → SettingChanged → Activity integration proof after Settings adoption exists — U1 deliberately has no Settings registrar/adopter, so loading Settings into a real tenant fixture before U3 must fail F6 readiness and Activity must not gain a Settings runtime dependency merely for tests — if wrong, the real integration proof must move earlier with its genuine prerequisites; no dummy Settings registration, fake active marker, or claim of completed tenant Settings integration is allowed.

Additional W1/U3 preflight: Activity has no Settings dependency or existing SettingChanged bridge. SettingSubjectReferenceData currently fixes type=nvl_setting and contains only id; SettingChanged adds key/revision/operation/context but captured tenancy is explicitly U3 work. W1 reference-protocol proof and U3 actual mutation/event bridge proof are now assigned explicitly. The actual U3 fixture must include/adopt Activity when its bridge is enabled, preserving native transaction lifetime and value-free logs.

## W1 read-only source preflight

Current Activity already uses DatabaseMigrations in its existing TestCase and routes only Feature tests; retain that topology and add the independent Tenancy test case. ActivityServiceProvider currently boots ModelActivityTimelineResolver with a static retained ModelActivityTimelineService; changing container singleton bindings alone will not remove this retained service graph. Include that existing bridge in the W1 lifetime review and prove sequential app/context use against current scoped context, preserving the trait-facing public interface. ActivityRelationLoader currently trusts preloaded relations and instantiates arbitrary valid stored model classes before querying their schema; enabled-mode registered canonical ownership and safe causer presentation must guard both paths. Existing disabled behavior remains covered by the original package tests. These are concrete locations under W1's stated scope, not authorization for broader timeline presentation refactors.

Ruling: W1 keeps Activity's normal migration default intact and executes the distinct `database/tenancy-migrations` ownership schema only through its selected package adoption adapter under the real coordinator; include that schema in the existing migration contract and quality inventories — current `optional_migrations` metadata describes an entirely optional primary migration set and changes its expected migrations.enabled default to false, while the current contract scanner only visits database/migrations and the quality runner only finds its immediate files — if wrong, the inventories may need a broader multiple-set representation; no automatic adoption at provider boot, no skipped migration recorded as applied, and no disabling Activity's existing normal migrations is allowed. Extend the owning existing inventory/tool and focused contract test only as required for this new path, rather than adding Activity to the primary-only optional_migrations map or creating a new suite-wide framework.

Concrete source locations: tools/validate-package-family.php currently derives the primary migration default at lines698–704 from optional_migrations; tools/check-package-contracts.php currently hashes only database/migrations at line692; tools/package-quality-runner.php currently discovers unreleased immediate migrations at lines429–441. The new ownership migration must not silently evade immutable migration inventories or static coverage. Existing released migration files remain unchanged.

Task 1: starting from reviewed75c8eff3cb7d2e18cd490209469370a4646afa26. Foundation F1–F8 and U1/U2 complete, all prerequisites reviewed. Fresh implementer will own serial runner. Existing Settings protocol/adoption-inventory rulings and static timeline bridge source preflight are included in task-1-brief.md.

W1 agent /root/tenancy_w1_implementation (Astra/high; first production adopter with manual/Spatie capture, canonical polymorphic hydration and deferred lifetime security) dispatched from75c8eff; sole serial runner. Brief task-1-brief.md, report expected task-1-report.md. No W1 implementation/review claim yet.

W1 implementer preflight confirms empty-store real adoption and explicit denial of nonempty legacy adoption untilW6. Deferred Settings-protocol proof uses package-local TenantQueuedJob event/listener plus actual serialization, no Settings runtime dependency. Global causers use configured model and fixed safe identity projection; controller clarified that configured causer display cannot override a registered tenant resource ownership policy, and unregistered arbitrary stored classes/preloaded payloads must not become a global-query escape. This is a concrete application of existing canonical ownership requirements.

Ruling: W1's dedicated DatabaseMigrations fixture loads the explicit ReflectionClass-resolved Tenancy core path in installed Testbench's defineDatabaseMigrationsAfterDatabaseRefreshed hook instead of the illustrative defineDatabaseMigrations hook — the earlier hook executes the core schema before migrate:fresh drops it, producing a missing audit-store setup failure rather than the required ownership failure — if wrong, the fixture hook must be adjusted to the real Testbench lifecycle; DatabaseMigrations, explicit migration paths, real coordinator adoption and no outer transaction remain mandatory. Do not count the setup failure as behavioral RED. The installed HandlesDatabases concern confirms the dedicated post-refresh hook. Carry this actual fixture lifecycle into later copied package fixtures.

W1 interrupted: Astra implementer ended with model-capacity error after partial uncommitted implementation, no report/commit. Root process check (escalated read-only ps after sandbox denial) confirms no orphaned test/static/generator process; only five existing private services remain. Original agent asked for evidence handoff only, no code/test actions. Replacement will continue preserved partial state with serial runner.
Ruling: Continue interrupted W1 on Sol/high with the same reviewed base and full brief, preserving partial code and requesting the original worker's historical evidence — the most-capable worker hit service capacity rather than a code/design blocker, and the architectural boundaries are already specified — if wrong, unresolved architectural questions or review findings require a later capability escalation; no evidence is fabricated and no unreviewed work is marked complete.

W1 recovery implementer /root/tenancy_w1_recovery (Sol/high) dispatched with full brief and preserved partial state; owns serial runner. Original agent is handoff-only. Full review range remains75c8eff..futureW1HEAD, and no fix round is consumed by this service-capacity interruption.

Original W1 handoff recovered and read in full: task-1-interrupted-handoff.md. Preserved true first behavioral RED after real adoption: recorder INSERT lacks non-null ownership_key, thenGREEN1test/2assertions. Current expanded5cases have4pass/13assertions and genuine hydrationRED (A expected, B secret preloaded relation returned). No historical evidence reconstruction needed. Recovery worker notified; original agent complete/handoff-only, no pending processes. Handoff identifies remaining deletion-time capture, safe causers, native deferred listener/worker, archive, inventories/docs/regressions/gates; none claimed complete.

Recovery source risk sent to implementer: partial ActivityOwnershipGuard::assertSubject uses newFromBuilder before assertRecord; installed Eloquent fires retrieved there, so synthetic identifier normalization can invoke observers before ownership admission. Require side-effect-free identifier holder preserving canonical class/table/actual Connection checks and a focused foreign-denial retrieved-observer assertion. Root performed read-only named inspection, no production fix. This is within existing supplied-model authorization scope.

W1 recovery checkpoint: freshly reproduced current focused ActivityTenantTest4/5passing,13assertions; hydration still returns preloaded B secret instead of A. Canonical event reload/guarded hydration and retrieved-observer regression are the next implementation pass. No new completed gate or commit.

W1 recovery milestone: incremental task-1-report.md read. Canonical event reload/guarded hydration, side-effect-free subject identifier admission, and scoped weak-map pre-delete ownership capture now pass7/7tests,18assertions. Recorded additional actual deletionRED7cases6pass16assertions before capture fix. Safe actor projection/timeline host resolver, deferred listener, remaining lifecycle/immutability/adoption tests and release gates still pending. No commit/review yet.

W1 recovery next milestone:11/11focusedtests31assertions reported after fixed safe global-causer projection, registered tenant-causer reclassification denial, tenant-scoped timeline host resolution and a native queued listener fixture carrying value-free nvl_setting reference/envelope. No blocker. Remaining named checks include immutable Activity ownership/Platform-envelope denial and retained timeline resolver scope refresh, then adoption/inventory, standalone archive and full gates. Exact worker/process evidence to be checked in final report; this milestone alone is not a standalone consumer claim.

Controller read-path quality handoff: preserve ActivityRelationLoader's existing type-grouped batch behavior while adding canonical event reload. Avoid per-event SQL admission/reload where a scoped batch query can validate the collection, and keep a multi-row query-budget check with the bounded installation probe measured separately. Sent to recovery worker within W1 read scope; no root production edits or broad refactor requested.

W1 recovery batching milestone: implementer reports16/16focusedtests45assertions;12Activity rows hydrate through1Activity data query and1subject data query, with fixed bounded schema metadata reads accounted separately. Report update requested/underway. Native sync queued-listener serialization evidence remains distinct from pending child-process absent-Auth archive proof. No commit/review yet.

W1 inventory milestone: recursive migration discovery for existing database/migrations, distinct database/tenancy-migrations and phased database/tenancy paths now has reproduced omissionRED then1/1GREEN6assertions per implementer. Activity adoption/schema and nonempty legacy denial are the next named checks; standalone/archive and full gates still pending.

W1 incremental adoption report read: focused selected-adoption test1/1GREEN7assertions verifies normal migrations remain enabled, tenant_id/non-null ownership_key and ownership/time index exist through real empty-store coordinator adoption. Once a fact exists, adapter prepare explicitly denies inferred historical assignment. Full legacy migration/retention remains W6; no release/standalone claim.

W1 standalone milestone: implementer reports actual archive child-process1/1GREEN14assertions(2.821s), selecting Activity+Tenancy+Data+SupportZIPs with offline dependencies, cached config and independent Composer loader, proving absent Auth/Suite, real adoption and identical-reference A/B isolation. This is distinct from native sync listener serialization tested in-process. Full Activity/root/static/style gates and commit/review still pending.

W1 reported broad gates: migration/root contracts36/36GREEN1084assertions; Activity covering108/108GREEN361assertions after eliminating disabled installation reads. Controller read the report and narrowly inspected that stated optimization: ActivityOwnershipGuard::attributes now returns before Boundary admission, and the global query scope similarly skips disabled admission. This contradicts frozen execution contracts §2, which requires the lazy actual-Connection marker probe even when disabled and explicitly measures it separately from query budgets. Worker instructed to restore guard, update fixed-cost budget characterization and add real adopted→disabled recorder/query/hydration denial. The108-test passing state is not accepted as completion/security evidence. No review fix round consumed; still implementation.

The same named supplied-model normalization check confirms assertSubject copies getKey into a fresh canonical instance without validating the supplied table/actual Connection or persisted original identity. Worker instructed to preserve those canonical checks and distinguish a just-created automatic Spatie subject from a forged dirty persisted identity, with focused regressions and no retrieved-event pre-admission side effect. This implements existing contract requirements; no root production edits.

W1 correction report read: focused4/4GREEN24assertions covers forged supplied subject table/actual Connection/dirty persisted primary key without retrieved observers, adopted→disabled recorder/query/direct hydration denial, and separately warmed/measured bounded legacy compatibility probe. Earlier108/361green from skipped admission is explicitly retained only as superseded diagnostic evidence. Final covering gates still pending; no W1commit/review.

W1 full-package checkpoint: implementer reports225/225GREEN829assertions after correcting empty multi-subject input validation to return before constructing a query; nonempty queries remain scoped. Formatpassed; canonical package-quality wrapper child exited1silently, while exact generated-config PHPStan passed0errors. Worker reminded to preserve separate evidence and use established F8 unchanged whole canonical wrapper outside sandbox for final gate if needed; no runner/suppression changes or wrapper-pass claim from child-only result. Final contracts/family/dependency/documentation/self-review/commit/review pending.

W1 incremental distribution gates read: package contracts unchanged against updated21-package snapshot; family validates21distributions; dependency audit all21 clean. Affected root TenancyConsumerWorkflow/PackageMigrationQuality/PackageQualityWorkflow40/40GREEN1163assertions9.561s. Final wrapper evidence/docs/self-review/commit remain pending; report Remaining work still to reconcile.

Ruling: Keep the tenant purge dispatcher/retention implementation in W6 — its explicit file/task list owns PurgeActivityLogsJob, purge Actions/commands, tenant locks and bounded per-tenant dispatch, while W1 is the minimal recording/read prerequisite — if wrong, retention must move forward before Activity can be called tenant-ready. W1 documents the enabled purge limitation and proves native dispatch denial through the existing purge Action without Queue fakes or deleted facts; disabled retention remains unchanged. No Platform envelope, global-job allowlist or implicit tenant enumerator is introduced.

Task1 implementation committed807e789d073395ee1540021790d2a36f91875beb; clean worktree verified. Full task-1-report.md read. Canonical Activity quality outside sandbox passes Pint/PHPStan0/full225tests829assertions; final added tenancy coverage20/20/63; affected root40/1163; composition45/2046; contracts/family/all21dependency audits pass. Native retention denial1/2confirmed, W6limitation recorded. Report distinguishes superseded108/361diagnostic and native in-process listener from archive child process. No W1completion yet.

Task1 review dispatched /root/tenancy_w1_review (Astra/high) over full75c8eff..807e789, review package168445bytes. Inputs task-1-brief/report and review diff; output task-1-review.md. Read-only, no broad reruns; request runner for any specifically justified focused check. Fix round0/5.

W1 reviewer checkpoint: diff read complete; two named unchanged-code checks are canonical persisted global-causer admission and tenant partition retention inside the suggestion Activity subquery. No runtime tests requested and no production mutations. Await both final verdicts.

Task1 independent review read in full: task-1-review.md, Spec❌/quality Needs fixes. I1Important: native lazy/explicit/eager Spatie subject/causer relations bypass guarded loader, exposing foreign subject or full global causer. I2Important: registered causer original-key admission can differ from current key associated, and global causers only class-admitted. NoCritical/Minor. Controller confirmed no native-relation API exclusion and routed both together to recovery implementer.

Task1 fix round1/5 started with /root/tenancy_w1_recovery, FIX_BASE807e789d073395ee1540021790d2a36f91875beb. Sole serial runner; targeted regressions, appropriate covering Activity tests, style/static/contracts and scoped commit. No broad host-model global hook, future retention work or unrelated suite rerun authorized. Await fix evidence/commit then scoped re-review.

W1 Cannot-verify resolution: historical TDD evidence is retained in original interrupted handoff and final recovery report, both read by controller, with freshly reproduced hydrationRED and subsequent focusedGREEN. The canonical wrapper outside-sandbox pass and final225/829 plus20/63 evidence are explicitly reported; silent sandbox failures remain separate. Reviewer correctly made no independent rerun claim. Actual other-engine Activity adoption/lifecycle and asynchronous retry are future W6proof, not W1completed evidence; F6/F7engine/worker foundations were independently reviewed on their prior commits. No missing W1proof established by that cross-task qualification.

Task1 fix1 milestone: implementer reproduced5tests0pass8assertions: native foreign subject hydration, unknown type construction/query, native causer secret/full fields, dirty registered causerID, invalid global causer acceptance. Selected bounded Activity-owned MorphTo subclass plus only ActivityLog subject/causer overrides, eager-parent batch admission/type allowlist/query predicates/projection, and creating-hook canonical causer association. Controller confirmed bounded approach and named installed HasAttributes::getRelationValue563–570 loaded-relation early return; worker must cover loaded-native subject/causer admission/projection without losing dedicated-loader batching. No root production changes.

Task1 fix1 first implementation run reported6tests5pass28assertions1error. Native foreign/injected loaded subject rejection, safe native/preloaded global projection, registered dirty-causer denial, canonical global-causer cases and dedicated-loader batch budget pass. Remaining unknown-type eager denial occurs during Laravel relation construction outside the current assertion closure; worker is distinguishing test evaluation shape from lifecycle defect before green claim.

Task1 fix1 focusedGREEN reported6/6tests32assertions: native lazy/explicit/eager foreign-subject denial, unknown type pre-construction denial, injected loaded subject/global-causer invalidation, safe native global projection, dirty registered-causer rejection, transient/nonexistent/forged/dirty global causer denial plus valid global case, preserved dedicated-loader batch budget. Covering gates/fixcommit/scoped re-review still pending.

Task1 fix1 covering evidence read: complete tenancy file24/24tests85assertions; named recorder/capture/timeline/suggestion/safety/API files91/91tests311assertions. Static first exposed14type errors, reduced to7around invariant MorphTo override types/eager-load arrays. Worker is resolving the Activity-owned interception at morphTo() rather than duplicating Spatie subject()/causer() overrides; final runtime verification must follow that adjustment. No behavioral blocker, no final static/commit claim yet.

Task1 fix1 static milestone: targeted ActivityLog/ActivityMorphTo/ActivityOwnershipGuard PHPStan0errors. Final selected shape supersedes the intermediate morphTo interception: ActivityLog owns typed subject()/causer() returning ActivityMorphTo; enabled mode guards, disabled mode uses native loading/model creation after the required persisted-adoption probe. Final behavioral/covering checks and full affected static result still pending.

Ruling: Retain the focused named ActivityMorphTo implementation with @internal and intentionally update only Activity's discovered-symbol contract snapshot using the existing tool — the guarded native relation fix requires an additive implementation class, while the scanner includes internal named classes and an anonymous embedded replacement would substantially worsen readability/static complexity — if wrong, consumers may interpret a discovered internal symbol as supported API; keep internal status explicit and preserve public recorder/relation compatibility. No scanner exclusion or unrelated baseline change. Worker reports final behavior and full PHPStan green; exact report/commands/output and final contracts are still required before scoped review.

Task1 fix1 committed89d889c9b0e2a115198bc5019bde81231612ab1d, clean worktree. Full appended fix report read with exact final commands/output: tenancy24/85, covering6feature files91/311, required native subset5/25 after final relation refactor, Pint/PHPStan0/contracts pass. No suppressions; Activity-only symbol snapshot acknowledged. Scoped re-review /root/tenancy_w1_fix1_review (Astra/high) dispatched over807e789..89d889c,53488bytes; findingsI1/I2 plus new fix breakage only, report task-1-fix-1-review.md. No task completion yet.

Task1: fix round1/5 (1 addressed, 2 open — I2canonicalcauser addressed; I1/R1trusted relation cache survives sequential TenantRunner A→B in the same scope; new ImportantR2per-relation synchronization drops the eager-loaded sibling; commits807e789..89d889c). Full task-1-fix-1-review.md read; no out-of-scope observations. Reviewer confirmed native HasAttributes early return, TenantRunner context swap without scoped-service reset, and sequential Laravel eager relation loading via bounded source checks; no runtime reruns.

Task1 fix round2/5 resumed /root/tenancy_w1_recovery, FIX_BASE89d889c9b0e2a115198bc5019bde81231612ab1d. Exact open findings referenced from scoped review, trueRED tests required for retained native/dedicated loaded relation A→B and both combined eager relation orders preserving serialization/no lazySQL. Covering tenancy/timeline/recorder/API, Pint/affectedmaxstatic/contracts; no unrelated gates. Sole serial runner remains worker.

Task1 fix2 trueRED reported against89d889c:2tests0passed6assertions. Retained native/dedicated A subject or registered causer reads fail to throw after A→B; combined eager loading loses first relation at relationLoaded assertion. Worker applying trust tokens bound to context and canonical identity, preserving only admitted sibling relations during canonical synchronization. No design blocker or GREEN claim yet.

Task1 fix2 focusedGREEN:ActivityTenantTest filter trusted native|combined native passes2/2tests22assertions475ms after0/2RED6assertions. Worker reports trust binds current ownership plus exact Activity class/table/actualConnection/key/existence identity; canonical sync preserves only sibling relations with matching tokens. Named covering gates and scoped commit/re-review still pending.

Task1 fix2 committed93aba799e4cc0c2fabcd39422836606e14ba1d78, exact HEAD independently verified and clean. Worker message expanded the short SHA incorrectly; use this git-verified full hash. Full appended report read:focused2/22GREEN aftertrueRED; final named covering71/268 (tenancy/timeline/recorder/API), Pint/maxPHPStan0/contracts unchanged. Existing12-row batch budget included. Scoped re-review resumed /root/tenancy_w1_fix1_review over89d889c..93aba79,18041bytes, findingsI1/R1,R2plus newfixbreakage only; output task-1-fix-2-review.md. W1stillawaitsverdict.

Task1: fix round2/5 (2 addressed, 0 open — I1/R1trust now binds current boundary attributes/canonical storage identity; R2restores only admitted sibling relations after batch reload; commits89d889c..93aba79). Full task-1-fix-2-review.md read:bothADDRESSED,no newbreakage,noout-of-scopeobservations; no runtime reruns. I2remainsaddressed.
Task 1: complete (commits75c8eff..93aba79, review clean). W1 minimal Activity prerequisite is complete; nonempty legacy adoption/tenant retention remain explicitlyW6 and actualSettingsintegrationU3.

## Implementation-first continuation — W2-W6

The user explicitly required all remaining implementation surfaces before any runtime verification. Accordingly, authored tests below are implementation artifacts only; no RED/GREEN, suite, consumer, matrix, independent review, PHPStan, Pint, contract, package-validation, or broad verification result is claimed.

- W2 implemented in `00d9d41`: canonical Forms ownership for definitions, entries, submissions, analytics, origins, throttling, spam, public site/token resolution, captured callbacks, and real adoption.
- W3 implemented in `12ebbf1` and `1be8894`: Content-owned snapshot/copy seam, tenant Template graphs/grants/copies, MediaCatalogImport staging/root rollback, output isolation, and real adoption.
- W4 implemented in `9c31a39`: tenant Comments targets/replies/reactions/mentions/projections/attachments, host/Auth global-principal projection adapter without Comments→Auth dependency, and real adoption.
- W5 implemented in `09ad3ce`: tenant Mail scheduling/tracking/factories/provider callbacks, persisted envelopes, fresh factory resolution, U3 tenant SMTP Settings, deployment-managed platform credentials, and real adoption.
- W6 authored: Activity and Mail bounded maintenance worklists, tenant-partitioned retention/recovery, serialized purge envelope coverage, adoption registrations, root workflow ownership graph, production-consumer/CI/docs/skills/distribution surfaces, and explicit implementation report.
- Deferred commands: every plan test command, behavioral end-to-end journey, consumer, database/cache/storage/SMTP/worker matrix, race rehearsal, independent review, PHPStan, Pint, contracts, package validation, Composer/archive install, and config/route cache run is **UNRUN (deferred by user)**.
- `tools/package-contracts.json` refresh is **UNRUN and intentionally deferred by user**.
- Completion record: `.superpowers/sdd/2026-09-16-tenancy-workflows/workflows-implementation-report.md`.
