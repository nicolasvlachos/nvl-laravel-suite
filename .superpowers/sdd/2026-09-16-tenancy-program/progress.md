# SDD ledger — plan: docs/superpowers/plans/2026-09-16-tenancy-program.md

Approved execution started from a8c5b97 in .worktrees/tenancy on codex/configurable-tenancy.
Per-plan records live in sibling plan-specific SDD workspaces; do not recreate completed tasks after compaction.

Order: F1–F7 → U1–U2 → F8 → W1 → L1–L5 / A1–A10 → R1–R5 → P1 → R6–R9 → C1–C6 → W2–W6 / U3–U6 → P2.
Current: PAUSED for user-requested 5.6 takeover. F1–F8, U1/U2, W1 and L1 are complete/reviewed: 12 of 52 tasks. HEAD a3a9387d1ed4cdd2144fdf6f0336ab8c874b6bbe. L2 remains uncommitted, final package gates green, eager-matching interpretation and independent review pending. Read docs/superpowers/plans/2026-09-16-tenancy-5-6-handoff.md and the L2 report before resuming. No implementation or test process remains active; five private infrastructure services were left in place, health not rechecked.
Runtime tests are serialized; the active implementer owns the runner unless transferred to controller explicitly.
No external dependency changes, remote pushes, deployment, or merge to main authorized by this execution.

Controller environment check: root composer requires PHP ^8.4 / Laravel ^13.0 and Spatie Laravel Permission ^8.0. Downstream Auth work must inspect installed v8 APIs/docs instead of assuming an older major. No dependency edits made by this check.

Read-only infrastructure preflight during F2: PHP has pdo_pgsql, pdo_mysql, pdo_sqlite and redis extensions. Docker CLI exists at /usr/local/bin/docker, but its default daemon socket /Users/nicolasvlachos/.docker/run/docker.sock is absent. Herd services:list reports Pro required. No service started or dependency changed. Existing CI uses PostgreSQL 17, MySQL 8.4 and MariaDB 12.3; no existing Redis/MinIO service definition found in the two package workflows. Provision isolated services when the real database/Redis/S3 proof gate is reached; do not count these unrun gates as passing.

Settings/Tools workspace initialized for future U1/U2; preflight table and two rulings there, including the F2/U3 transaction-lifetime reconciliation. Read it when entering U1. No Settings/Tools implementation started; do not confuse prepared briefs with complete tasks.

Read-only infrastructure follow-up: local PostgreSQL 17.6 and Redis 8.10.0 are available; MySQL is 9.5.0 and cannot stand in for the required supported MySQL 8.4 gate. No MariaDB/MinIO/container runtime binary was found. Initialized an isolated PostgreSQL data directory under /private/tmp with private Unix socket directory, host authentication rejected and test-only identity; infrastructure.json records the exact disposable paths. No developer database or globally installed service configuration is modified. Database still requires startup/test proof; no engine tests claimed yet.

Infrastructure initialization initially failed because the sandbox prohibits shmget; escalated retry succeeded. PostgreSQL 17.6 now runs only on the private Unix socket with TCP disabled, and its disposable test database exists. Socket access also requires escalated test execution. Exact paths/identity are in infrastructure.json; shutdown obligation remains with the program controller. This is infrastructure readiness only, no tenancy database proof yet.

Workflows preflight/exact task extracts prepared (no implementation). Its ledger records U3-before-W5 dependency from the required real tenant Settings/SMTP fixture. Read workflow ledger when entering W1.

Private PostgreSQL health query verified current_database/current_user nvl_tenancy_test, version 17.6 and empty listen_addresses (no TCP). Original main checkout remains clean. This check is not an engine migration test.

Translatable preflight/exact task extracts prepared during F4, no implementation. Its ledger preserves no-production-schema rule, disabled missing-declaration marker guard, and L5 copied-consumer/process requirements. Read it when entering L1.

Further infrastructure check found a separate Homebrew mysql@8.4 (8.4.7) outside PATH. First sandboxed initialization crashed in native memory setup; identical initialization with escalation succeeded, confirming the sandbox boundary was involved. Started private mysql84-test with TCP and MySQL X disabled and private Unix socket; created only nvl_tenancy_test DB and a DB-scoped test identity. Root bootstrap account exists only in that private cluster. No host service/default files used (--no-defaults). infrastructure.json records paths; controller must stop this instance too. This supersedes the earlier unavailable-MySQL note; no engine tests claimed yet.

Private Redis 8.10.0 is running with port 0, private Unix socket permissions 700 and persistence disabled; PING returned PONG. Exact socket/pidfile in infrastructure.json. Controller owns shutdown alongside PostgreSQL and MySQL. Docker.app is installed but its daemon is not started; it remains a possible later route for required MariaDB/S3-compatible gates. No image downloads/container startup performed.

Auth preflight and exact task extracts prepared during F4 re-review, no implementation. Read tenancy-auth/progress.md when entering A1: it records prerequisite ordering for schema fixtures, mandatory audit/context and canonical role validation. Intermediate readiness remains fail-closed.

Resources preflight/exact task extracts prepared during F5, no implementation. Read tenancy-resources/progress.md when entering R1: complete schema/fixture contracts must be appended to raw task extracts, grant model prerequisites move with their first graph registration, immutable mapping recovery supersedes an inconsistent R9 instruction.

Docker preflight: escalated desktop start --detach reported already running; actual desktop-linux daemon socket remains absent and info did not return, interrupted cleanly. Last available backend log was Sep7 engine crash due then-full disk; current filesystem has 22GiB available. No Docker settings reset, restart, image download, container or volume created. Existing app is not task-owned and must not be stopped during cleanup. MariaDB/S3 service gates remain unprovisioned; PostgreSQL/MySQL/Redis private test services unchanged.

Content/Sites preflight and six raw task extracts prepared during F5; no implementation. Read its ledger and append shared fixture/registration contracts before dispatch. C4 SEO fixture may require minimal C5 empty-adapter/schema prerequisites moved forward, no fake marker.

MariaDB now provisioned independently of Docker. Downloaded official Homebrew 12.3.3 arm64_tahoe bottle from historical formula commit396f590b8f25e823f35f49d813ff62ee853164be, verified SHA256 e7a682b64b9007ece217b05f3619057d76734022e8e7698cce2a6c0dd6da21d0, extracted only under private test root. Adjusted its private Mach-O placeholder dependency paths to existing /opt/homebrew pcre2/OpenSSL libraries and ad-hoc re-signed only those temporary executables; no Homebrew install/link/config edits. Private data/socket/process initialized with --no-defaults and --skip-networking, test-only DB/user created. Health reports12.3.3 and skip_networking1. Controller now must stop this fourth private server at finish. Exact paths in infrastructure.json. No MariaDB package gate run yet.

MinIO vendor direct binary endpoint returned410. Official archived repository documents source-only distribution; latest release API returns RELEASE.2025-10-15T17-29-55Z. Downloaded official Go1.27.1 darwin-arm64 archive, verified SHA256 ee215d57e0ec269c60cc9ceca68e6bda321ba9ee5afe24f4b0988703c2d87d12, extracted only under test root. MinIO source build is running as exec session19348 with GOPATH/GOMODCACHE/GOCACHE/GOBIN under same temp root, GOMAXPROCS2/CGO0. No project dependencies or host PATH modified. Poll build before starting S3 server; cleanup compiler/module caches at finish.

## Program task preflight

| Task/pair | Producer/consumer or internal check | Finding / resolution |
|---|---|---|
| P1 | Sealed archive, fixture CLI seed/verify, genuine worker | Fixture only, exact public Actions for membership/Media behavior. Missing envelope is injected into queued row for consumer denial; normal producer already rejects it. Full and Media-only profiles need separate temporary consumers. |
| P2 | Complete graph/matrix/lifecycle and dependency audit | P1 fixture extended after every focused gate. Cleanup uses explicit approved resource manifest and package APIs under maintenance lease, no generic tenant deletion API. Restore uses separate disposable DB/object state. |
| P1/P2 | Shared consumer runner/provider/config/contract/workflows | Extend P1 once; no duplicate sealed harness. Reuse established candidate archive variables and cleanup trap. P2 lifecycle/whole graph tests build on P1's seed/worker/verify phases. |
| F8/P1 | Distribution proof/archive manifest | Foundation is independently installed before full graph; P1 selects only integrated features and bridges. |
| A10/R5/P1 | Global principal memberships and separate owned media/bytes | Authoritative behavior uses public Actions, bytes and payload evidence; direct fixture rows only observations. |
| R9/C6/W6/U6/P2 | Complete resource/workflow/site/tools graph | Each proof owns its package writes and full-program consumer orchestrates APIs. No integration is silently declared ready because an inert provider loads. |
| All/P2 | Configuration/Doctor/skills/contracts/version counts |21 distributions; Support/Data/Filterable/Primitives retain neutral dependencies. Standalone absent Auth remains tested. |

Active re-adoption is a reviewed schema/config evolution mechanism, not cross-tenant transfer (outside first-release scope). Package adapters must reject unsupported changes and preserve canonical existing tenant assignments. This bound was sent to F5 implementer and appended to its brief.

MinIO source build completed successfully. go version -m verifies github.com/minio/minio v0.0.0-20251015172955-9e49d5e7a648, module sum h1:6TdolSCLSs2nwm8i0PpWDqf9iX2Ty9WQK8wmr7dCnUM=; releaseAPI tag RELEASE.2025-10-15T17-29-55Z. Binary --version labels normal go-install build DEVELOPMENT.GOGET; exact module evidence is authoritative. Running only on127.0.0.1:55196 with private data and generated test credentials stored mode0600 in minio-credentials.json (do not print credentials). Health /minio/health/ready returned200. Controller must stop fifth private service via minio.pid and clean task-owned compiler/data caches on finish. No S3 multipart/package test has run yet. All required SQL/Redis/S3 local service types are now available; Docker remains untouched after already-running response.

Prepared P1/P2 exact heading extracts with shared global constraints via heading-aware extraction (bundled task-brief script only recognizes Task N, while program uses literal P1/P2). No program proof task dispatched. Original main checkout still clean on latest read.

F6 supported-engine coordinator/migration proof is complete on PostgreSQL17.6, MySQL8.4.7 and actual MariaDB12.3.3 driver; infrastructure.json stale pre-proof status fields corrected. Cleanup instructions are in infrastructure-cleanup.md (not cleanup.md). Redis/S3 package gates remain future tasks; health alone is not proof.

DuringU1 read-only cross-package check: Auth and Media Composer requirements do not include Settings, and their providers have no Settings provider registration. Early minimal/Auth+Media fixtures can therefore exclude unfinished Settings while full Suite remains metadata-readiness guarded untilU3. Preserve explicit selected-provider/feature fixture topology, no bypass to activate an incompatible loaded fullsuite.

During U2 read-only capacity check: Data volume has 23 GiB free; task-owned SQL/Redis/MinIO/compiler root is 3.3 GiB and SDD records 2.4 MiB. No cleanup or service action needed now. Infrastructure cleanup remains required only at completion/cancellation; this capacity check is not service health or package proof.

W1 fixture preflight corrected installed Testbench hook timing: explicit core load now follows DatabaseRefreshed, preserving real DatabaseMigrations/coordinator. Controller verified vendor HandlesDatabases and propagated the bounded lifecycle handoff to future briefs that mention DatabaseMigrations; do not count setup failures as behavioral RED. W1 runtime implementation remains active.

User status intervention during W1: asked "what is really going on mate". Controller acknowledged excessive fragmentation and repeated micro-updates, reported exact10/52reviewed plus W1uncommitted, main clean/current75c8eff and no merge/deploy; Auth/Media/Metafields/Taxonomy integrations remain future. User did not cancel execution. Worker directed to close only required W1coverage/documentation/report/commit without scope expansion or redundant gates, then independent review. Canonical Activity wrapper outside sandbox is now confirmed green225/829,0PHPStanerrors; the previous silent failure remains diagnostic evidence.

Handoff 2026-09-16: User requested a comprehensive guide for 5.6 because of cost. Controller stopped implementation and obtained the complete L2 report; no L2 commit or review was made. User model/cost preference overrides automatic premium-model escalation. Preserve the dirty tree and ignored records. Guide, refreshed ruling index, and handoff-state.json describe exact resume/evidence/cleanup obligations. This is a handoff pause, not program completion or cancellation.

Resume 2026-09-17: Translatable L1-L5 are complete and independently reviewed. Current clean HEAD is a4230c86e52d4cf4e391b835ca974636ddfc226c (`test(translatable): tighten worker failure evidence`). Cross-package Media/Metafields/Taxonomy adoption remains owned by the downstream Resources plan; Translatable itself does not make tenant-linked host resources responsible for a second tenancy key.

User explicitly directed completion of every remaining program item and objected to repeated broad verification between unfinished microtasks. Execution is therefore grouped by coherent plan: focused RED/GREEN tests during implementation, one full package/runtime/database/archive matrix at the end of each plan, and one independent review for the completed plan. Next milestone is Auth A1-A10 as one serial batch, followed by Resources R1-R5, P1, Resources R6-R9, Content/Sites C1-C6, Workflows W2-W6, Settings/Tools U3-U6, and final P2. No merge, push, deployment, global-service mutation, or infrastructure cleanup is authorized before final completion/cancellation.

Auth A1-A10 completed through `ba2e505` and received final independent approval: all original and follow-up Critical/Important findings closed, zero new findings. Focused portable/native-engine/HTTP/consumer/static gates are recorded in the Auth report and reviews. Previously disclosed legacy broad-PostgreSQL timestamp failures and downstream Translatable dependency findings remain explicitly outside the Auth verdict.

Resources R1-R5 Media implementation is committed through `5a2e852`; its first whole-plan review rejected with 2 Critical, 14 Important and 1 Minor finding. One consolidated Media correction wave is active. Do not mark R1-R5 reviewed/complete until the corrective implementation, final affected/native matrix and scoped re-review close every finding. Combined Auth/Media package-contract baseline refresh remains intentionally deferred until both active code surfaces are final.

User cadence override after excessive multi-session verification/review churn: finish the entire remaining implementation scope first with one explicit ledger; do not run per-finding tests, repeated package matrices or independent plan reviews while implementation remains incomplete. After all remaining code is implemented, run one consolidated verification matrix, fix all failures as one batch, then run the consolidated matrix once more. Status updates must stay limited to completed implementation blocks, real blockers and final evidence. This supersedes the earlier per-plan matrix/review cadence.

P1 implementation completed 2026-09-17 from clean base `7e413b622120b22c277186e90082a9b5ed9808de`: a sealed full Auth+Media Suite consumer and physically separate standalone Media-only/no-Auth consumer now exercise the fixture CLI seed/verify phases, real public tenant/Auth/Media actions, equal-byte ownership/path isolation, scalar queue envelopes and failure/missing-envelope observations, legacy mapping with interrupted/resumed adoption, process restart, cached unsafe-downgrade denial, and SQLite/native PostgreSQL+Redis+S3 workflow wiring. Release proof consumers now include tenancy. Per the user cadence override, no tests, consumer run, matrix, independent review, PHPStan, Pint, or package validation ran in this implementation turn; all behavioral evidence remains explicitly deferred to the consolidated verification phase. See `p1-implementation-report.md`.
